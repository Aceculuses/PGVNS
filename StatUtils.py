#!/user/bin/python3
import copy

class StatUtils:

#
    def symmetricalUncertainty(ctable):
        pass

    def differentValues(array):
        newArray=copy.deepcopy(array)
        newArray.sort()
                
